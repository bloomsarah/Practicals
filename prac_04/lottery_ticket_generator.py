number_of_picks = int(input("Enter number of quick picks: "))

# generate n lines of output
# each line has 6 random numbers between 1 and 45
# no repeated numbers on each line
# display numbers in sorted (ascending) order
# do not use the random sample() fxn
